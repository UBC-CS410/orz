package tests.client;

import junit.framework.TestCase;
import org.junit.Test;
import com.google.gwt.maps.client.geom.LatLng;

/**
 * JUnit test class for MapCoordinate.java
 *
 */
public class MapCoordinateTest extends TestCase
{
	private LatLng coordinateAsLatLng;
	
	/**
	 * Verify that the instance fields in the class are set correctly
	 */
	@Test
	public void testMapCoordinate()
	{
	/*	Double[] coordinateAsDoubles = new Double[2];
		
		LatLng coordinate = 
		
		MapCoordinate mc = new MapCoordinate(coordinate);
		
		assertNotNull(mc);
		assertEquals(coordinateAsDoubles, mc.getCoordinateAsArray());
		assertEquals(coordinateAsLatLng, coordinate); */
	}

}
